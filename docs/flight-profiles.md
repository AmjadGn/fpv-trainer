# Flight Profiles

`FlightProfile` drives mass, thrust, drag, inertia, rates, wind sensitivity, crash thresholds, and recovery.

`flightProfileToAppliedConfig` maps profiles into the single `FlightControllerService` without per-aircraft gameplay forks.

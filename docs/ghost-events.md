# Ghost Events

Ghost events publish compatible replay benchmarks for a fixed course, weather,
and physics version. Accepted ranked attempts are ranked by time; a faster
attempt marks the benchmark beaten and may award configured season points.
Practice attempts are retained but do not affect ranked limits or rankings.

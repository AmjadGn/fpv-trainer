# Competitive Aircraft Rules

`AircraftPolicy` kinds: unrestricted, fixedAircraft, allowedAircraftList, aircraftClass, normalizedPerformance, separateLeaderboardPerAircraft.

Ranked stamps include aircraftId + physics/collider versions. Do not mix incompatible craft on one board unless the event allows it.

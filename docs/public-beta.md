# Public Beta

Registration mode is configured with `FPV_BETA_MODE`. `open_registration`
permits normal sign-up; closed modes require a valid, enabled, unexpired
invite. Invites may bind an email address and have an enforced usage limit.
Never commit production invite codes or `.env` files.

import { Component, inject, signal } from '@angular/core';
import { FormsModule } from '@angular/forms';
import { ActivatedRoute, Router, RouterLink } from '@angular/router';
import { AuthApiService } from '../../core/auth/services/auth-api.service';
@Component({standalone:true,imports:[FormsModule,RouterLink],template:`<main class="online-panel"><h1>Choose a new password</h1><form (ngSubmit)="submit()"><input name="email" [(ngModel)]="email" placeholder="Email" type="email" required><input name="password" [(ngModel)]="password" placeholder="New password" type="password" required><input name="passwordConfirmation" [(ngModel)]="passwordConfirmation" placeholder="Confirm password" type="password" required><button>Reset password</button></form><p>{{message()}}</p><a routerLink="/login">Sign in</a></main>`,styles:[`.online-panel{width:min(520px,calc(100% - 2rem));margin:4rem auto;padding:2rem;border:1px solid var(--fpv-border);background:var(--fpv-panel)}input,button{display:block;width:100%;padding:.7rem;margin:.7rem 0;background:#0b1118;color:white;border:1px solid var(--fpv-border)}button{background:var(--fpv-accent);color:#081214}a{color:var(--fpv-accent)}`]})
export class ResetPasswordComponent { private api=inject(AuthApiService); private route=inject(ActivatedRoute); private router=inject(Router); email=''; password=''; passwordConfirmation=''; message=signal(''); submit(){this.api.resetPassword({email:this.email,token:this.route.snapshot.queryParamMap.get('token')??'',password:this.password,passwordConfirmation:this.passwordConfirmation}).subscribe({next:()=>this.router.navigateByUrl('/login'),error:()=>this.message.set('Unable to reset password.')});} }
